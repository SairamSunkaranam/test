 pipeline {

  agent any
  parameters {
      choice(name: "env", choices: "qa\nprod")
  }
  
  stages {
    stage ('test'){
       steps {
       
         script {
             echo "Hello world"
             echo "$env/${env}"
         
             if ("$env" == "qa" ) {
               QA_BUCKET = "test-value1"
               C3_PREFIX = "my-provate-value"
             }      
         
             echo "${QA_BUCKET}/${env}.tfstater"
             echo "${C3_PREFIX}/${env}.tfstater"
             
             echo "${WORKSPACE}"

         }
       }
    }
  }

}
